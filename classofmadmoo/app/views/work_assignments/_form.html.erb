<%= form_for(@work_assignment) do |f| %>
  <% if @work_assignment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@work_assignment.errors.count, "error") %> prohibited this work_assignment from being saved:</h2>

      <ul>
      <% @work_assignment.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="field">
    <%= f.hidden_field :project_id, :value => @project %>
  </div>
  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :body %><br>
    <%= f.text_area :body %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
